"""Гості середовища рушіїв.

Файли тут виконуються ЧУЖИМ інтерпретатором (середовищем рушіїв, де є
torch і strhub, але немає пакета) — тому жоден із них не імпортує `nyshporka`, а
пакет не імпортує їх. Це та сама межа, що в `htr/runner.py` і `htr/patches`;
приймач — `tests/test_train_guest_isolated.py`.
"""
