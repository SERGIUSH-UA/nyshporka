"""🧪 Лабораторія: розмітка рядків і навчання Писаря на своїх почерках.

Пакет дає готові ваги, але письмо в архівах не одне: писар іншої канцелярії
або інше десятиліття дають рушієві кожну п'яту літеру неправильно, і жодна
роздавана модель цього не закриє. Закриває це кілька сотень рядків, звірених
оком саме на цьому письмі, і дотренована на них модель. Досі цей конвеєр жив
поза пакетом; тут — його публічна форма.

Ланцюжок: `cut` (кропи рядків із прогону) → `voices` (голоси рушія) →
`export`/`import` (злиття арбітрами) і вкладка «Розмітка» (людина) →
`build` (корпус) → `start` (трен: своя карта, свій сервер або gpurunner) →
`eval` (епоху обирає holdout, не val) → `promote` (у бойові).

🔴 Усі операції — `section="lab"`, `agent=False`: в агентну поверхню вони не
йдуть ніколи, для агента є командний рядок (`nysh train …`).

🔴 Незамінне (мітки, кропи) лежить у `data/train/`, відтворюване (корпуси,
прогони) — у `data/derived/train/`. Правило з `setup/packs.py`: кеш система
має право прибрати, а мітки, звірені оком, — ні.
"""
from __future__ import annotations

__all__ = ["layout", "sets", "text"]
