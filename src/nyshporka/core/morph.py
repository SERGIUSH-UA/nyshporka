"""🔤 Морфологія прізвища — форми породжуються, а не переписуються руками.

Навіщо. Пошук прізвища в рукописному декоді стоїть на переліку його написань:
відмінки, роди, дореформена орфографія, польська латинка, розрив переносом.
Зібрати такий перелік руками — це десятки рядків на КОЖНЕ прізвище, і саме тому
готові інструменти пошуку по архівах працюють лише з тим родом, під який їх
писали.

Тому тут генератор: **основа + парадигма → повний набір форм**. Основа — це
дані (дослідник знає, як його прізвище пишеться в російській, українській і
польській традиції), парадигма — код (7 відмінків × 2 роди × множина × 5
орфографій ≈ 70 форм, які інакше довелося б виписувати вручну).

Приклад наскрізь: «Сикорский» / «Сікорський» / «Sikorski».

🔴 Що НЕ узагальнюється і чому саме так:

* **Основа на кожну орфографію задається явно.** Рос. «Сикор-» → укр. «Сікор-»:
  перша «и» стає «і», а «и» в закінченні лишається. Позиція залежить від історії
  слова, не від правила, тож автоматична згортка помиляється частіше, ніж
  допомагає — а ціна помилки тут мовчазна: половина написань просто не шукається.
* **Орфографія `bank` — не мова, а таблиця підписів.** Вона може бути внутрішньо
  непослідовною (наз. відмінок дореформений поруч із сучасним родовим), якщо так
  склалось історично в конкретному дослідженні: це мітки, на які вже спирається
  зроблена розмітка, і «полагодити» їх означало б тихо перетегувати її.
  Генератор відтворює таблицю як є.
* **Описки не виводяться.** Те, як помиляється писар (зайвий м'який знак,
  переставлені літери), правилом не породжується; такі форми лишаються явним
  переліком у профілі дослідження.
"""
from __future__ import annotations

from dataclasses import dataclass, field

#: Порядок відмінків — він же порядок підписів у банку розмітки.
CASES: tuple[str, ...] = ("nom", "gen", "dat", "acc", "ins", "loc", "voc")
GENDERS: tuple[str, ...] = ("m", "f")

#: Орфографічні шари. `bank` — не мова, а історична таблиця підписів (див. докстрінг).
ORTHOGRAPHIES: tuple[str, ...] = ("bank", "ru_modern", "ru_prereform", "uk", "pl")


@dataclass(frozen=True)
class Paradigm:
    """Закінчення на кожну орфографію. Основа підставляється зліва."""

    id: str
    label: str
    #: орфографія → код форми («nom_m», «gen_f», «gen_p») → закінчення
    endings: dict[str, dict[str, str]] = field(default_factory=dict)
    #: Чи парадигма перевірена на живому матеріалі, чи лише заготовка.
    verified: bool = False

    def form(self, stem: str, code: str, orth: str = "bank") -> str | None:
        table = self.endings.get(orth)
        if table is None or code not in table:
            return None
        return stem + table[code]

    def forms(self, stem: str, orth: str = "bank") -> dict[str, str]:
        table = self.endings.get(orth) or {}
        return {code: stem + end for code, end in table.items()}


# ── парадигми ────────────────────────────────────────────────────────────────
# Прикметникова на -ський: Сікорський, Ярошинський, Левицький, Осадовський.
# Найпоширеніший тип у шляхетських і духовних родах Правобережжя, тому єдиний,
# що звірений із живими таблицями роду (див. тести).
_ADJ_SKYI = Paradigm(
    id="adj_skyi",
    label="прикметникова на -ський/-ский/-ski",
    verified=True,
    endings={
        # 🔴 Історична таблиця підписів банку — з її ж непослідовністю:
        # наз. дореформена («-скій»), а род./знах. сучасні («-ского»).
        "bank": {
            "nom_m": "скій", "gen_m": "ского", "dat_m": "скому", "acc_m": "ского",
            "ins_m": "скимъ", "loc_m": "скомъ", "voc_m": "скій",
            "nom_f": "ская", "gen_f": "ской", "dat_f": "ской", "acc_f": "скую",
            "ins_f": "скою", "loc_f": "ской", "voc_f": "ская",
        },
        "ru_modern": {
            "nom_m": "ский", "gen_m": "ского", "dat_m": "скому", "acc_m": "ского",
            "ins_m": "ским", "loc_m": "ском", "voc_m": "ский",
            "nom_f": "ская", "gen_f": "ской", "dat_f": "ской", "acc_f": "скую",
            "ins_f": "ской", "loc_f": "ской", "voc_f": "ская",
            "nom_p": "ские", "gen_p": "ских",
        },
        "ru_prereform": {
            "nom_m": "скій", "gen_m": "скаго", "dat_m": "скому", "acc_m": "скаго",
            "ins_m": "скимъ", "loc_m": "скомъ", "voc_m": "скій",
            "nom_f": "ская", "gen_f": "ской", "dat_f": "ской", "acc_f": "скую",
            "ins_f": "скою", "loc_f": "ской", "voc_f": "ская",
            "nom_p": "скіе", "gen_p": "скихъ",
        },
        "uk": {
            "nom_m": "ський", "gen_m": "ського", "dat_m": "ському", "acc_m": "ського",
            "ins_m": "ським", "loc_m": "ському", "voc_m": "ський",
            "nom_f": "ська", "gen_f": "ської", "dat_f": "ській", "acc_f": "ську",
            "ins_f": "ською", "loc_f": "ській", "voc_f": "ська",
            "nom_p": "ські", "gen_p": "ських",
        },
        "pl": {
            "nom_m": "ski", "gen_m": "skiego", "dat_m": "skiemu", "acc_m": "skiego",
            "ins_m": "skim", "loc_m": "skim", "voc_m": "ski",
            "nom_f": "ska", "gen_f": "skiej", "dat_f": "skiej", "acc_f": "ską",
            "ins_f": "ską", "loc_f": "skiej", "voc_f": "ska",
            "nom_p": "scy", "gen_p": "skich",
        },
    },
)

# ⚠ Нижче — заготовки. Механіка та сама, але на живому матеріалі вони НЕ
# перевірялись: рід автора прикметниковий, і зміряти якість не було на чому.
# Перший, хто візьме їх у роботу, має звірити з реальною справою й підняти
# `verified`, інакше вони дають правдоподібний, але неперевірений перелік.
_NOUN_OV = Paradigm(
    id="noun_ov",
    label="присвійна на -ов/-ев (Івановъ, Кузьминъ)",
    endings={
        "bank": {
            "nom_m": "ъ", "gen_m": "а", "dat_m": "у", "acc_m": "а",
            "ins_m": "ымъ", "loc_m": "ѣ", "voc_m": "ъ",
            "nom_f": "а", "gen_f": "ой", "dat_f": "ой", "acc_f": "у",
            "ins_f": "ой", "loc_f": "ой", "voc_f": "а",
        },
        "ru_modern": {
            "nom_m": "", "gen_m": "а", "dat_m": "у", "acc_m": "а",
            "ins_m": "ым", "loc_m": "е", "voc_m": "",
            "nom_f": "а", "gen_f": "ой", "dat_f": "ой", "acc_f": "у",
            "ins_f": "ой", "loc_f": "ой", "voc_f": "а",
            "nom_p": "ы", "gen_p": "ых",
        },
        "ru_prereform": {
            "nom_m": "ъ", "gen_m": "а", "dat_m": "у", "acc_m": "а",
            "ins_m": "ымъ", "loc_m": "ѣ", "voc_m": "ъ",
            "nom_f": "а", "gen_f": "ой", "dat_f": "ой", "acc_f": "у",
            "ins_f": "ой", "loc_f": "ой", "voc_f": "а",
            "nom_p": "ы", "gen_p": "ыхъ",
        },
        "uk": {
            "nom_m": "", "gen_m": "а", "dat_m": "у", "acc_m": "а",
            "ins_m": "им", "loc_m": "і", "voc_m": "е",
            "nom_f": "а", "gen_f": "ої", "dat_f": "ій", "acc_f": "у",
            "ins_f": "ою", "loc_f": "ій", "voc_f": "о",
            "nom_p": "и", "gen_p": "их",
        },
    },
)

_INDECLINABLE = Paradigm(
    id="indeclinable",
    label="незмінна (Шевченко, Мітітєл, Бігар)",
    endings={
        orth: {f"{c}_{g}": "" for c in CASES for g in GENDERS} | {"nom_p": "", "gen_p": ""}
        for orth in ORTHOGRAPHIES
    },
)

PARADIGMS: dict[str, Paradigm] = {p.id: p for p in (_ADJ_SKYI, _NOUN_OV, _INDECLINABLE)}


def paradigm(pid: str) -> Paradigm:
    try:
        return PARADIGMS[pid]
    except KeyError:
        raise KeyError(
            f"невідома парадигма «{pid}». Є: {', '.join(sorted(PARADIGMS))}") from None


# ── переноси ─────────────────────────────────────────────────────────────────
def hyphenate(word: str, cut: int, joiner: str = " ") -> tuple[str, str, str]:
    """Слово, розірване переносом → (повний запис, голова, хвіст).

    Повертає всі три, бо в декоді вони зустрічаються ОКРЕМО: голова «Доли-» на
    кінці рядка, хвіст «щинский» на початку наступного, а разом вони читаються
    лише вікном. Саме тому пошук цілого слова сліпий до переносів — 2 з 4
    прізвищ у сповідці 1846 розірвані через рядок.
    """
    if not 0 < cut < len(word):
        raise ValueError(f"розріз {cut} поза межами слова «{word}» ({len(word)} літер)")
    head, tail = word[:cut] + "-", word[cut:]
    return f"{head}{joiner}{tail}", head, tail


# ── таргети для fuzzy-пошуку ─────────────────────────────────────────────────
#: Скільки літер відрізати від голови на кожному щаблі й з якою вагою.
#: 🔴 Це generic-правило, а не властивість прізвища: рушій калічить довге слово
#: сильніше за коротке, тож повний корінь мусить конкурувати з власними хвостами.
#: Ваги дисконтують частковий збіг; береться max по щаблях (`anchor.htr_match`).
TARGET_LADDER: tuple[tuple[int, float], ...] = ((0, 1.0), (2, 0.85), (4, 0.65))
#: Голова переносу окремим таргетом: сама по собі це слабкий сигнал.
HEAD_TARGET: tuple[int, float] = (4, 0.45)


def htr_targets(root: str,
                ladder: tuple[tuple[int, float], ...] = TARGET_LADDER,
                head: tuple[int, float] | None = HEAD_TARGET,
                ) -> tuple[tuple[str, float], ...]:
    """Нормалізований корінь → драбина таргетів «корінь + його хвости».

    `root` подається вже нормалізованим (мала літера, без ѣ/ъ/ь/й) — щоб цей
    модуль не тягнув за собою нормалізатор пошуку.
    """
    out = [(root[skip:], w) for skip, w in ladder if skip < len(root)]
    if head is not None:
        n, w = head
        if n < len(root):
            out.append((root[:n], w))
    return tuple(out)
