"""🤝 Обмін прочитаним: спакувати свій декод і прийняти чужий.

Одну й ту саму архівну книгу сьогодні розпізнає кожен окремо. Прогін коштує
недорого, але він коштує ЩОРАЗУ, і на корпусі в тисячі аркушів це вже гроші й
доба часу — витрачені на текст, який у сусіда давно лежить.

Модуль робить із прочитаного переносний пакет (`bundle`), доводить, що пакет не
брехливий (`gates`), чесно називає, наскільки текст ліг на кадри отримувача
(`align`), і лишає слід того, що звідки прийшло (`journal`).

🔴 Межа модуля: їде ТЕКСТ рушія, не дослідження. Сховище сторінок, вердикти,
канон і нотатки лишаються вдома — див. білий список у `bundle.PACKED`.
"""
