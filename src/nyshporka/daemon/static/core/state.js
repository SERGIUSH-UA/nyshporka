/**
 * Стан, який переходить МІЖ екранами. Усе, що живе в межах одного
 * екрана, лишається в його модулі.
 *
 * 🔴 Один об'єкт, а не окремі змінні: імпортована змінна доступна
 * імпортерові лише на читання, тож передача «пошук → гортач» мовчки не
 * спрацювала б — гортач відкрився б порожнім замість знайденого рядка.
 */
export const ST = {
  /** Прогін і сторінка, відкриті в гортачі. */
  view: null,
  /** Справа, відкрита в обліку прочитаного. */
  eye: null,
  /** Знахідки, віддані в розбір. */
  sift: { hits: [], i: 0, q: '', crop: null, ctx: null },
  /** Тека справи, підставлена в форму читання. */
  read: null,
  /** Ключ справи, до групи якої гортати перелік прогонів. */
  runsFocus: '',
  /** Справа, аркуші якої треба відкрити. */
  frames: null,
  /** Чим засіяти пошук: справа, у межах якої шукати. */
  search: null,
  /** Куди відкрити реєстр опису: {repo, fond, spr} — із бібліотеки чи газетира. */
  fonds: null,
  /** Яку справу показати в бібліотеці: {key} — точний спільний ключ. */
  library: null,
};
