# 🔴 Фікстура злиття зберігається ПОБАЙТОВО, без нормалізації кінців рядків —
# і вхід, і золото. Реєстр пишеться з явним LF, а покриття через `write_text`,
# тобто на Windows із CRLF; нормалізація git зробила б золото нерівним самому
# інструменту на тій самій машині, а вхід — різним на різних машинах, і тест
# падав би на чистому клоні без жодної зміни коду.
tests/data/merge_fond/** -text
